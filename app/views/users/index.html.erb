<% provide(:title, 'All users') %>

<h1>All users: <%= @users.count %></h1>

<div class = "pageLinks">
	<%= will_paginate %>
</div>

<ul class = "users">
	<%= render @users %>
</ul>

<div class = "pageLinks">
	<%= will_paginate %>
</div>